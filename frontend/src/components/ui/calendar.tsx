// Stub Calendar component retained to avoid breaking imports during migration.
// The original implementation used react-day-picker and was removed as part
// of the react-datepicker migration. This lightweight stub renders nothing
// and should be removed once all references are fully migrated.

export function Calendar(_: any) {
  return <div />
}

export function CalendarDayButton(_: any) {
  return <button />
}
